//  import * as React from 'react'
//  import * as enzyme from 'enzyme'
//  import CodeEditor from './index';
//  import Form from './../../index'

describe('<CodeEditor />', () => {
  it('U-TEST-1 - Test JavaScript', () => {
    // const wrapper = enzyme.mount(
    //   <Form>
    //     {(_) => (
    //       <CodeEditor name="editor" mode="javascript" label="JavaScript Code Editor" />
    //     )}
    //   </Form>
    // )
    // expect(wrapper.find(React.Fragment)).toHaveLength(1)
    // expect(wrapper.find(React.Fragment).get(1).props.mode).toEqual("javascript")
  })
})